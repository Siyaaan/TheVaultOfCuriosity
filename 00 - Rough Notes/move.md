ohh, so the move put the data on the onkeydown, because without it, only at that line where onkeydown is stay the data. and without it, it doesn't pass down on the following lines

move is a keyword in rust which applies the ownership principle. 
